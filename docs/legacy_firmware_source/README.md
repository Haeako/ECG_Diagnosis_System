# Legacy firmware source

These directories duplicate older filter/network/OS implementations. The active
ESP-IDF build uses `firmware/esp32/main/components/*`; none of the source in this
archive is listed by `firmware/esp32/main/CMakeLists.txt`.
